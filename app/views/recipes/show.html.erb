<h1><%= @recipe.name %></h1>

<ul>
    <% @recipe.ingredients.each do |ing| %>
        <li><%= link_to ing.name, ingredient_path(ing) %></li>
    <% end %> 
</ul>

<%= form_tag recipe_path(@recipe), method: "delete" do%>
    <%= submit_tag "Delete this recipe"%>
<% end %>